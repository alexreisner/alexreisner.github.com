= To-do List

* add sidebar/contact info to non-post pages (incl home)
* bleed image on post pages into mail column (set as div#doc bg?)
* RSS feed

* design
  * use home page layout for all non-post pages
  * refactor post page design
  * syntax highlight colors


